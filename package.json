{
  "name": "whatsupcloud-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready full stack website for WhatsUpCloud AI Training Institute.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
